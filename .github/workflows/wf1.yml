name: GitHub Actions Eslint

on: 
  push:
    branches: [main]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
      - run: ls
      - run: npm install
      - run: eslint ./*
